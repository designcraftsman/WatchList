@model IEnumerable<WatchList.Models.ModelViewFilm>

<h1>My Film List</h1>

<!-- Button to Add a New Film -->
<div class="mb-3">
    <a asp-action="Create" asp-route-id="">Add</a>
</div>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Year</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var film in Model)
        {
            <tr>
                <td>@film.Title</td>
                <td>@film.Year</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@film.IdFilm">Edit</a> |
                    <a asp-action="Details" asp-route-id="@film.IdFilm">Details</a> |
                    <a asp-action="Delete" asp-route-id="@film.IdFilm">Delete</a>
                    <button class="btn btn-primary add-film" data-id="@film.IdFilm">Add to Watchlist</button>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script>
        $(document).ready(function () {
            // Handle "Add to Watchlist" button click
            $(document).on('click', '.add-film', function () {
                const idFilm = $(this).data('id'); // Retrieve film ID

                $.post('/FilmList/AddFilm', { idFilm: idFilm })
                    .done(function (response) {
                        alert(response); // Notify user of success
                    })
                    .fail(function (xhr) {
                        alert(`Failed to add film: ${xhr.responseText}`); // Show error message
                    });
            });
        });
    </script>
}

